include: "Common.snake"
include: 'Concoct.snake'
include: 'Assembly_map.snake'
include: 'Annotation.snake'
include: 'megahit_graph.snake'

rule all:
    input: expand("{group}/annotation/contigs_{annotation}.tsv", annotation=ANNOTATION,group=GROUPS),
           expand("{group}/graph/contigs.gfa",group=GROUPS),
	       expand("{group}/graph/{file}",group=GROUPS,file=GRAPH_FILES)
          # expand("{group}/binning/clustering_merged.csv", group=GROUPS)

